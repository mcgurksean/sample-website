# My Sample Website

This is a simple static website synchronized into Kubernetes using git-sync.

## Structure
- `index.html` – homepage
- `about.html` – about page
- `assets/` – static assets (CSS, JS, images)
- `config/` – optional configuration files consumed by the application

## Git-Sync Usage
This repository is pulled by a git-sync sidecar in Kubernetes.
